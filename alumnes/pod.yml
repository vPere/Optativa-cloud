---
apiVersion: v1
kind: Pod
metadata:
  name: redis
spec:
  containers:
  - name: redis
    image: redis
    volumeMounts:
      - name: redis-storage
        mountPath: /usr/share/nginx/html
    volumes:
    - name: nfs-pv
